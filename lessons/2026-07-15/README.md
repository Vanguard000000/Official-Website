# July 15 — Computer-Vision Performance

Open [the lesson](index.html) in a browser, then work in:

`mediapipe-lab/sims/pose-estimation/index.html`

Today is one measured change: stop constructing the export artifact every camera
frame. Write the prediction in `plan.md` first and the five before/after runs in
`results.md` after testing.
